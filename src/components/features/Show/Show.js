import React from 'react';

const Show = () => (
  <form>
    <label>Sort By</label>
    <select>
      <option>Price: Lowest first</option>
      <option>Price: Highest first</option>
    </select>
  </form>
);

export default Show;
